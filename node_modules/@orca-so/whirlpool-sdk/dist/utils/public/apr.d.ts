import { PoolData } from "../../types";
import { TokenUSDPrices } from "../token-price";
export declare type EstimatedAprs = {
    fee: number;
    rewards: number[];
};
export declare const ZERO_APR: {
    fee: number;
    rewards: number[];
};
export declare function estimateAprsForPriceRange(pool: PoolData, tokenPrices: TokenUSDPrices, fees24h: number, tickLowerIndex: number, tickUpperIndex: number): EstimatedAprs;
